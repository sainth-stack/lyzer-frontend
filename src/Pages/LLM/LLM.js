/* import React from 'react';
import TextInput from './TextInput'; // Adjust the path according to your folder structure
import SelectInput from './SelectInput'; // Adjust the path according to your folder structure
import { modelOptions } from '../../data/DataJson';

const LLM = () => {

    return (
        <div className="p-4">
            <h2 className="text-xl font-bold mb-4">Configure your LLM:</h2>
            <TextInput label="Model Vendor" placeholder="openai" />
            <TextInput label="LLM API Key" placeholder="Enter API Key" />
            <SelectInput label="Model" options={modelOptions} />
        </div>
    );
};

export default LLM;
 */